/**
 * @format
 */

import { AppRegistry } from 'react-native';
import EcolutionApp from './EcolutionApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => EcolutionApp);
